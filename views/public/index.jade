extends layout

block content
  each s in sections
    a(name= s.id)
    case s.type
      when "jumbotron"
        include ./includes/jumbotron
      when "product"
        include ./includes/product